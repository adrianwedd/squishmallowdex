usage: squishmallowdex.py [-h] [--limit N] [--batch-size N]
                          [--download-images] [--stats-only] [--out FILE]
                          [--csv FILE] [--images-dir DIR] [-v] [--adventure]
                          [--no-adventure] [--no-color] [-q] [--log FILE]
                          [--phoenix] [--delay SEC] [--batch-delay SEC]
                          [--cache DIR] [--progress-file FILE] [--refresh]

🧸 SQUISHMALLOWDEX 🧸
Build your own searchable Squishmallow database!
Like a Pokédex, but for Squishmallows. Works offline too!

options:
  -h, --help            show this help message and exit

🧸 Collection Options:
  --limit N             How many Squishmallows to catch (0 = catch 'em all!).
  --batch-size N        Save progress every N catches (default: 30).
  --download-images     Download pictures for offline viewing (uses more
                        space).
  --stats-only          Just show collection stats, don't download anything
                        new.

📁 Output Files:
  --out FILE            HTML file to create (open in browser).
  --csv FILE            CSV file to create (open in spreadsheet).
  --images-dir DIR      Folder for downloaded images.

🎨 Display Options:
  -v, --verbose         Show more details (default: -vv). Use -q to reduce.
  --adventure           Adventure mode with fun facts (default: on).
  --no-adventure        Turn off adventure mode (less chatter).
  --no-color            Turn off colors (for terminals that don't support
                        them).
  -q, --quiet           Minimal output (just errors and final summary).
  --log FILE            Log file to write all output (default:
                        squishmallow.log).
  --phoenix             Show the epic phoenix celebration art at the end!

⚙️  Advanced Options (for power users!):
  --delay SEC           Wait time between downloads (be nice to servers!).
  --batch-delay SEC     Extra wait time between batches.
  --cache DIR           Folder for cached web pages.
  --progress-file FILE  File that tracks which pages we've visited.
  --refresh             Re-download everything (ignores cache).

🌟 QUICK START EXAMPLES 🌟
─────────────────────────────────────────────────────────
  Catch your first 50 Squishmallows:
    python squishmallowdex.py --limit 50

  Full adventure mode with images (takes a while!):
    python squishmallowdex.py --adventure --download-images

  See everything that's happening:
    python squishmallowdex.py -vv --adventure

  Just check your collection stats:
    python squishmallowdex.py --stats-only

💡 TIP: You can stop anytime with Ctrl+C and resume later!
        Your progress is saved after every batch.
